// https://www.interviewbit.com/problems/power-of-2/
divide = function(A, B) {
    var remainder;
    var a = A.split("");
    var divisor = parseInt(B);
    var quotient = [];
    var dividend = 0;

    while(a.length > 0) {


    }

    if (remainder != 0) {
      return false;
    }
};

power = function(A) {
  var ret = 0;
  var a = A.split("");



  return ret;
};

console.log(divide("123", "5"));

console.log(power("2"));
console.log(power("127"));
console.log(power("128"));
